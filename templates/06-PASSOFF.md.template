# PASSOFF — {{PROJECT_NAME}} (personal board, untracked)

**What is next.** One row per item, one standalone prompt per item below the board. What is
true lives in `HANDOFF.md`.

Status vocabulary: `OPEN` · `IN FLIGHT` · `DONE — HANDOFF n` · `HELD` · `SETTLED AS NO` ·
`SUPERSEDED`.

| # | Task | Status | Model | Waits on | Files it owns |
|---|------|--------|-------|----------|---------------|
| 1 | <!-- first task --> | `OPEN` | Default | — | — |

---

### 1. <!-- Task title -->

*Opened {{DATE}}, out of <!-- what prompted this -->.*

**Model: <!-- Default, or a specific model if the task calls for one -->.**

<!-- A standalone prompt: enough context that a fresh session with no memory of this
     conversation can pick it up. State why it exists, what is fixed and must not be
     relitigated, what to do in order, what to ask before building, what is out of scope, and
     what to hand back when done. -->
