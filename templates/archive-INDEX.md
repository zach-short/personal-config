# archive-{{PROJECT_NAME}} — index

Docs pulled out of the `{{PROJECT_NAME}}` repo when they closed. One folder per topic, flat, no
nesting. **This is a map, not a copy:** status lives in the files themselves and can drift out
of date here. What an archive owes a reader is accurate pointers, not a uniform shape.

Legend: ✅ closed/shipped · 🟡 built, not runtime-verified · ⚪ planned/open · 🔍 audit (findings only)

<!-- One line per archived folder: topic, what it was, its last commit, the date verified.
     A folder with no line here is invisible; a line pointing at a moved folder is worse than
     none. `personal-config doctor` checks both directions. -->

## Closed

<!-- - **<slug>/** ✅ — what it was. Closed <date>, last commit `<hash>`. -->
