---
name: close-out
description: Run the end-of-work ritual — update the record, then post the hand-back blocks. Use when {{CLOSE_OUT_TRIGGER}}, when work is being landed early on budget, or when the user says "close this out", "wrap up", or "hand off".
---

# /close-out

{{WORK_UNIT}} ends the same way, in this order, without being asked. The point is that the
owner can close the session immediately after: set the stated model, paste one block, go.

## 1 — Update the record{{RECORD_WHERE}}

{{RECORD_STEPS}}

## 2 — Post {{BLOCK_COUNT}} blocks in the chat

Not only in the docs — the whole value is that they are copy-pasteable at the moment the
session ends.

{{HAND_BACK_BLOCKS}}

## 3 — State plainly what was and was not verified

{{PROOF_SENTENCE}}
